apiVersion: v1
kind: Service
metadata:
  name: {{ include "shelly-prometheus-exporter.fullname" . }}
  labels:
    {{- include "shelly-prometheus-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shelly-prometheus-exporter.selectorLabels" . | nindent 4 }}
